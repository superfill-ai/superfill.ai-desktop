import { readAISettings, readUISettings, writeAISettings, writeUISettings } from "./handlers";

export const settings = {
    readAISettings,
    writeAISettings,
    readUISettings,
    writeUISettings,
};
